# BOT Generator

A starter Python Telegram bot project for receiving user requests and generating reports.

 Set your bot token in an environment variable:
   ```bash
   ```
Run the bot:
   ```bash
   python init.py
   ```

## Notes

- `main.py` contains a minimal bot skeleton.
- `requirements.txt` includes packages for Telegram integration and PDF generation.
- Extend the bot with a database, request processing, and report generation as needed.
